# Example .env file for aws-agentcore-agent
# Copy this file to .env and fill in the values.

# -- AWS Configuration --
AWS_REGION="ap-southeast-2"
# AWS_PROFILE="your-aws-profile" # Optional: if you use a specific AWS profile

# -- Agent Configuration --
# AGENT_NAME="MyAgent"
# AGENT_ID="AGENT_ID" # from `terraform output`
# AGENT_ALIAS_ID="AGENT_ALIAS_ID" # from `terraform output`

# -- OAuth Configuration (for A2A Inspector) --
# CLIENT_ID="" # from `make oauth-config`
# TENANT_ID="" # from `make oauth-config`
# TOKEN_URL="" # from `make oauth-config`
# AUTHORIZATION_URL="" # from `make oauth-config`
# SCOPE="" # from `make oauth-config`
